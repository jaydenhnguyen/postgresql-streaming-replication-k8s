# Evidence

Terminal transcripts from local dry runs (CLO835 Project 3).

| File | What it shows |
|------|----------------|
| `01-bootstrap.txt` | Timed bootstrap from a clean kind cluster → streaming + seed |
| `02-lag-check.txt` | LSN / `lag_bytes` idle vs under write load (pause replay) |
| `03-practice-promote.txt` | Practice promote + `count(*)` / `max(id)` reconcile on both pods |

No passwords or Secret values are included.
