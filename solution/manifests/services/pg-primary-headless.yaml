apiVersion: v1
kind: Service
metadata:
  name: pg-primary-hs
  namespace: pg-${STUDENT_ID}
  labels:
    app: postgres
    role: primary
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app: postgres
    role: primary
    student-id: ${STUDENT_ID}
